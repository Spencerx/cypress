{
  "name": "@packages/example",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build-prod": "yarn build",
    "build": "node ./bin/build.js && gulp build",
    "clean-deps": "rm -rf node_modules",
    "deploy": "gulp deploy",
    "postinstall": "echo '@packages/example needs: yarn build'",
    "predeploy": "yarn build",
    "test-e2e": "cypress open",
    "test-e2e:ci": "cypress run",
    "test-integration": "exit 0",
    "test-integration:ci": "exit 0",
    "test-unit:ci": "cross-env NODE_ENV=test mocha",
    "test-unit": "yarn test-unit:ci --watch --inspect"
  },
  "files": [
    "cypress",
    "lib"
  ],
  "dependencies": {
    "bluebird": "3.5.3",
    "glob": "7.1.3"
  },
  "devDependencies": {
    "chai": "3.5.0",
    "cross-env": "5.2.1",
    "cypress-example-kitchensink": "1.8.1",
    "gulp": "4.0.2",
    "gulp-clean": "0.4.0",
    "gulp-gh-pages-will": "0.5.5",
    "gulp-rev-all": "2.0.2",
    "mocha": "2.5.3",
    "resolve-pkg": "2.0.0",
    "shelljs": "0.8.3"
  }
}
